# Movie-finder
A beginner-level movie search application built using React.js and the OMDb API. It allows users to search for movies, view movie details in a popup modal, and browse results through a responsive interface.
